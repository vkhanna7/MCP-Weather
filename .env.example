# Copy this file to .env and fill in values if you extend the demo
# to use a real weather API.

# Optional: OpenWeatherMap API key (not needed for the mock demo)
# OPENWEATHER_API_KEY=your_key_here

# Optional: Anthropic API key (only needed if you wire in a real LLM client)
# ANTHROPIC_API_KEY=your_key_here
